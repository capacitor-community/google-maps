/**
 * A data class representing a pair of "width" and "height" in pixels.
 */
export interface MarkerIconSize {
  /**
   * Width in pixels
   *
   * @default 30
   * @since 2.0.0
   */
  width: number;
  /**
   * Height in pixels
   *
   * @default 30
   * @since 2.0.0
   */
  height: number;
}
