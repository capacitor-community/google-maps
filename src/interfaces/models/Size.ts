/**
 * A data class representing a pair of "width" and "height".
 */
export interface Size {
  /**
   * Width
   *
   * @since x.x.x
   */
  width: number;

  /**
   * Height
   *
   * @since x.x.x
   */
  height: number;
}
